# requirements-llm.txt
# Optional: Local LLM dependencies for Qwen3-1.7B inference.
# Only installed via: bash scripts/setup_llm.sh
# NOT included in the default lightweight requirements.txt
#
# Important:
# Do NOT install this file directly with plain pip on AMD ROCm machines.
# accelerate depends on torch, so a direct "pip install -r requirements-llm.txt"
# can still resolve to the default CUDA torch wheel through a transitive
# dependency. Always use scripts/setup_llm.sh, which installs torch from an
# explicit ROCm or CPU index before installing accelerate:
#   AMD ROCm: bash scripts/setup_llm.sh --rocm
#   CPU only: bash scripts/setup_llm.sh --cpu

-r requirements.txt
transformers>=4.51.0
accelerate>=0.26.0
safetensors>=0.4.2
huggingface_hub>=0.23.0
